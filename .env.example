# Authentication provider selection
# Tests and local development default to simple auth; production stays on Clerk.
AUTH_PROVIDER=simple

# Simple auth configuration (required when AUTH_PROVIDER=simple)
# Point to a YAML file defining local users. Relative paths resolve from repo root.
SIMPLE_AUTH_USER_FILE=apps/api/tests/shared/simple-auth/users.yaml

# Clerk Authentication (Required when AUTH_PROVIDER=clerk)
# Get these from https://dashboard.clerk.com
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
# CLERK_SECRET_KEY=your_clerk_secret_key_here

# Optional: Override default ports
# PORT=5001
# VITE_PORT=5173

# Optional: Database path
# DATABASE_PATH=./data/ctrl-freaq.db

# Optional: CORS settings
# CORS_ORIGIN=http://localhost:5173

# Optional: Rate limiting
# RATE_LIMIT_WINDOW=900000
# RATE_LIMIT_MAX=100
# RATE_LIMIT_ENFORCEMENT_MODE=reject # options: reject, log

# Event stream (SSE) hub feature flags
# The SSE hub is disabled by default. Opt-in with ENABLE_EVENT_STREAM=true on
# the API and VITE_ENABLE_SSE_HUB=true on the web client. Override advanced
# behaviour with the additional knobs below.
# ENABLE_EVENT_STREAM=false
# EVENT_STREAM_REPLAY_LIMIT=100
# EVENT_STREAM_HEARTBEAT_INTERVAL_MS=15000
# EVENT_STREAM_MAX_RETRIES=5
# VITE_ENABLE_SSE_HUB=false
